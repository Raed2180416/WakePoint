# GeoWake — Re-Verified Working Backlog (BACKLOG_CURRENT.md)

> Supersedes the file:line references in `GAP_ANALYSIS.md`, which have drifted.
> Built from 8 gap-family agents that re-read the **current** code on branch `sim-validation`.
> Every row below is **still-valid** or **partially-fixed**. Items graded already-fixed/stale are in the drift section at the bottom.
> Ranking: **BLOCK → HIGH → MED**; within a tier, provable-headless items first (they are tonight's work).

## Ranked backlog

| # | Gap | Sev | Current file:line | Current behavior | Fix | Provable headless? |
|---|-----|-----|-------------------|------------------|-----|--------------------|
| 1 | Blackout alarm eval early-returns → cold-start-underground silent no-wake | **BLOCK** | `lib/services/tracking/location_stream_handler.dart:548-555` (guards), called from `:446` | `_maybeEvaluateAlarmDuringDropout` is the ONLY driver during a GPS blackout; hard-returns at `:549-550` if no prior fix and `:554-555` if no EKF. Rider who opens app already underground never reaches `checkAndTriggerAlarm` → reachability anchor never seeded → **never wakes**. | Split the blackout driver: keep EKF dead-reckon eval where an EKF exists, but add an **unconditional wall-clock never-late tick** that seeds the reachability anchor at arm/trip-origin and fires purely on `boundNow` (physics bound) with no dependency on `_lastEkfState`/`_lastGpsUpdate`. Remove the two returns as gates on the physics path only. | ✅ Yes — arm, feed zero positions, advance fake clock past `V_LINE*t`, assert fire. |
| 2 | Reachability anchor seeded on first tick / falls back to origin, not at arm time | **BLOCK** (partially-fixed) | `lib/services/tracking/alarm_controller.dart:411-412` (seedColdStart), `:420-424` (onAcceptedFix), reset `:272`; `reachability.dart:392-399` | Seeds at `progressMeters ?? 0.0` (already snapped arc, idempotent via `_anchor ??=`). But seeds on FIRST TICK reaching `checkAndTriggerAlarm`, not at arm; falls back to origin when `progressMeters` null; and per #1 the underground rider never reaches the tick → anchor never seeded, `boundNow` returns null. | Seed the anchor explicitly in arm/`startTracking` (or right after `resetAlarmState` `:272`) from first real on-route projection, flagged `fromRealFix:false` if origin guess. Guard the `?? 0.0`; ensure a wall-clock driver exists so `boundNow` can force a fire. | ✅ Yes — arm mid-route (`progressMeters=5000`), assert `anchor.sMeters==5000` not 0. |
| 3 | Reach bound wired ONLY to `evaluateCoinciding`; distance / non-metro time / 60% / geofence get zero physics protection | **BLOCK** | `alarm_controller.dart:1115-1158` (compute), passed only to `evaluateCoinciding` `:1182`; distance fires raw `:607` & `:890`; non-metro time `:793-827`; geofence `:1419`/`:1439`; non-metro 60% `alarm_evaluator.dart:1184` | `reachBoundMeters` is computed inside `_evaluateWithRoute` and handed only to `evaluateCoinciding`. Distance mode fires on raw `progressMeters` and returns before the reach block; non-metro time fires on raw ETA fractile; geofence uses raw straight-line; 60% uses raw `progressForThreshold`. Four non-metro/distance/geofence paths have no physics net. | Hoist `reachBoundMeters` up into `checkAndTriggerAlarm` before the distance branch; gate EVERY fire on `Reachability.effectiveProgress(...)`: distance (`:586`/`:875`), non-metro time (symmetric ETA lower-bound `remaining/V_LINE`), geofence (pass reachBound in), and `alarm_evaluator:1184`. | ✅ Yes — per mode: freeze progress, advance clock, assert fire when physics bound alone crosses target. |
| 4 | Preflight `block` verdict computed but **never enforced** — dialog is dismissible, arming is unconditional | **BLOCK** | `lib/screens/homescreen.dart:1038-1043` (preflight+dialog awaited), `:1045-1052` (`startTracking` runs unconditionally); dialog `reliability_preflight_runner.dart:40-85` ("Proceed anyway" `:80`); computation correct at `reliability_preflight_service.dart:264-344`, block issue `:275-285`, `isBlocked` `:131` | `PreflightResult` is discarded — nothing between `:1043` and `:1045` inspects `isBlocked`. Dialog's only action is `Navigator.pop()`. Rider with **notifications off** (the sole block-severity issue) still arms an alarm that physically cannot appear. Computation itself is sound; defect is 100% downstream. | Capture `preflight`; change dialog to return `Future<bool>`; on `isBlocked && !proceed` `setState` un-armed and `return` BEFORE `startTracking`. Keep warn/advisory proceeding. (Note: only notifications-off blocks today; DND is not probed at all.) | ✅ Yes — `FakeReliabilityProbe(notifications:off)` → assert `startTracking` not called. |
| 5 | Cross-state routes hard-blocked → interstate overnight sleeper cannot be armed | **BLOCK** | `homescreen.dart:263-314` (`_validateSameState`), invoked `:741-746`, awaited `:784`, hard block+`return` `:791-801` | `_validateSameState` reverse-geocodes origin/dest and returns false when states differ; arm flow shows "Route Not Available" and `return`s, never reaching `startTracking`. Flagship Delhi→Jaipur / Mumbai→Ahmedabad refused at the door — directly contradicts the G18 "no time-cap for long single-stop sleepers" comment at `:812`. | Remove the hard block: delete the `sameState` gate (`:741-746`, `:784`, `:791-801`) or invert into a non-blocking soft confirm ("long inter-state trip — continue?", default proceed). Never `return` out of arm on cross-state alone. | ✅ Yes — drive arm with cross-state origin/dest, assert `startTracking` still called. |
| 6 | `versionCode` hardcoded to 1 → second Play upload rejected | **BLOCK** | `android/app/build.gradle:22` (`versionCode 1`), `:23` (`versionName "1.0"`) | Literals, not wired from Flutter `flutter.versionCode`/`versionName`. Every build is versionCode 1, so no wake-path fix can ship after the first release. | Load from `local.properties`: `def flutterVersionCode = localProperties.getProperty('flutter.versionCode') ?: '1'` etc.; set `versionCode flutterVersionCode.toInteger()` / `versionName flutterVersionName`; drive from pubspec `version: 1.0.0+N`. | ✅ Yes — build-config assertion / gradle property read. |
| 7 | No persisted/network telemetry sink; `configure()` never called with a real sink | **BLOCK** | `lib/services/telemetry/telemetry_service.dart:76` (InMemory sink), `:112` (default list), `:116` (configure def) | Only concrete sink is `InMemoryTelemetrySink` (2000-cap RAM). Default list is `[InMemoryTelemetrySink()]`; `configure()` is defined but never called in `lib/` (only in tests). Every event evaporates on process death — the exact kill the funnel exists to measure. | Add `FileTelemetrySink` (append `toJsonLine()` to JSONL under `getApplicationSupportDirectory()`, batched fail-open flush); register via `configure()` in `main.dart` AND in the FGS isolate entrypoint `_onStart` (separate singleton). Optional `NetworkTelemetrySink`. | ✅ Yes — FileSink write/rotate/flush + configure-registration unit-testable with temp dir + fake clock. |
| 8 | `alarmOutcome` (on-time/late/missed) — the north-star metric — has **zero emit sites** | **BLOCK** | `telemetry_service.dart:203` (method def), `classifyOutcome` `:277` — no callers | `grep '\.alarmOutcome(' lib/` returns nothing. The alarm firing never records whether it fired on time vs late. The single most important field metric is never captured. | At the fire decision in `alarm_controller` compute `lead = trueArrivalTs - fireTs` and call `alarmOutcome(outcome: classifyOutcome(lead), marginSeconds, gpsLostSeconds, firedViaBackstop, firedViaReachability, mode)`. Also emit on backstop-fire and dismissed/snoozed paths. | ✅ Yes — drive evaluator with synthetic fixture, assert `sink.countOfType('alarm_outcome')==1` with expected margin sign. |
| 9 | V_LINE city never plumbed into `boundNow`; unknown line falls to `defaultMps` (28 m/s), not ceiling → fast RRTS late-fire | **HIGH** (partially-fixed) | `alarm_controller.dart:1134-1138` (no `city:`), `:1122`; `reachability.dart:108-114` (forLine), `:53` (default 28), `:65`/absoluteCeiling 56, `:77-101` (looksRrts/looksExpress); iOS path `ios_backstop_planner.dart:232` DOES pass city; lineName src `transfer_utils.dart:982` | RRTS/express keyword detection now exists (`looksRrts`/`looksExpress`), so partially fixed. But (1) `city` is still never passed (always null); (2) unknown-line fallback is still `defaultMps=28` not `56`; (3) lineName is `short_name`-first so brand token ("Namo Bharat"/"RapidX") in `name` can be missed → a ~44 m/s train bounded at 28 m/s fires LATE. | Pass leg `city:` into `boundNow` (mirror iOS `:232`); change `forLine` fallback `:113` from `defaultMps` to `absoluteCeilingMps=56`; match on both `short_name` and `name`; emit telemetry warn on any `defaultMps` resolution. | ✅ Yes — `forLine(city:'delhimeerutrrts')` ≥ rrtsMps(53); unknown line returns 56 not 28. |
| 10 | Backstop lead hardcoded 60s for stops/distance modes; never derived from reach bound | **HIGH** | `lib/services/tracking/notification_updater.dart:191-200` (flat `60.0` at `:199`); `scheduleEtaBackstop` `notification_service.dart:891-935` | `_maybeRearmEtaBackstop` uses `value*60` for minute/time modes else hardcodes `leadSeconds=60.0`. No reach/bound reference in the file; drives entirely off `smoothedETA`/`apiEtaSeconds` — exactly the "stale ETA" the design warns against. | Replace flat 60 with real lead: stops-before → `value * interStopSeconds`; distance-before → `(value_km*1000)/V_LINE_mps`. Thread the reach bound from `alarm_controller`'s `ReachabilityTracker` into `BroadcastContext` and prefer it over raw ETA. | ✅ Yes — feed config + fixture, assert derived `fireInSeconds`. |
| 11 | ETA backstop id `991` left armed after End Tracking (isolate dead) — not cancelled in bg handler, not in swept ids | **HIGH** | `notification_service.dart:1663-1682` (bg END_TRACKING cancels 0/888/889 at `:1677-1679`), `cancelAllNotifications:1466-1507` (sweeps 0/888/889/8888 `:1492-1496`, NOT 991), 991 const `:884`; only `trackingservice.dart:925` cancels it | `notificationTapBackground` END_TRACKING (short-lived isolate) cancels only 0/888/889, never 991. `cancelAllNotifications` also skips 991. `cancelEtaBackstop()` runs only from `_onStop` (needs live isolate). If process is dead, 991 stays armed → spurious full-volume wake after the trip. | Add `await plugin.cancel(991)` in the bg END_TRACKING branch (`~:1680`); add `991` to `cancelAllNotifications` (`~:1496`) and to the test-mode recorded-cancels list. | ✅ Yes — invoke bg END_TRACKING path, assert 991 in cancelled set. |
| 12 | No runtime/CI validation gate on shipped metro data; validator audits an unshipped JSON | **HIGH** | `tools/validate_metro_data.py:15` (audits `metro_dataset.json`); shipped data `lib/data/metro_line_sequences.dart:15`, `lib/all_india_stops.dart:5`; `pubspec.yaml:91` bundles only `assets/osm/` | Shipped Dart constants have zero assert/schema check. Validator audits `assets/india_metro/metro_dataset.json`, which is NOT bundled and never `rootBundle`-loaded — downstream of the Dart, free to drift. CI is Qodana-only; validator never runs. | Add a Dart unit test asserting invariants on the SHIPPED constants (monotone along-line, hop < ~6km, India bbox, no <40m dups, city/line key exists in `allIndiaStops`). Optionally regen the dataset from Dart in CI and fail on git diff. Wire validator into CI. | ✅ Yes — pure Dart `flutter test`. |
| 13 | Reroute metro whitelist excludes TRAM/COMMUTER_TRAIN, contradicting `transfer_utils` kMetro predicate | **HIGH** | `lib/services/reroute_constraints.dart:175-179` vs `lib/services/transfer_utils.dart:280-285` | Reroute accepts only `{SUBWAY, HEAVY_RAIL, RAIL, METRO_RAIL, MONORAIL}`; a valid alternate on TRAM/COMMUTER_TRAIN is rejected → tracking terminated. `_isMetroTransitStep` additionally accepts TRAM + COMMUTER_TRAIN. No shared constant. | Extract one shared `const Set kMetroVehicleTypes` (incl. TRAM, COMMUTER_TRAIN) and test membership in both predicates. | ✅ Yes — unit test both predicates against the shared set. |
| 14 | `reliability` funnel (FGS-survived / OS-killed / Doze / backstop-fired) has **zero emit sites** | **HIGH** | `telemetry_service.dart:220` (def, no callers); near-miss `alarm_controller.dart:1150` emits the SEPARATE `reachability` event under a "Reliability funnel" comment | `grep '\.reliability(' lib/` is empty. No FGS-restart detection, no Doze hook, no "backstop actually fired" emit. The OS-kill signal the funnel exists for is never sent. | On `_onStart`, detect restart-after-kill via a persisted clean-shutdown flag (absent → `osKilled:true`) and emit `reliability(...)`. Hook the process-death backstop receiver to emit `backstopFired:true`. Optional Doze broadcast listener. | ✅ Yes — restart-flag state machine is deterministically unit-testable. |
| 15 | `gpsLost` / `gpsReacquired` / `ekfHealth` have **zero emit sites** | **HIGH** | `telemetry_service.dart:191` (gpsLost), `:195` (gpsReacquired), `:246` (ekfHealth) — all no callers | Location-stream/EKF layer never reports fix gaps or cold-start/phantom rejections. GPS-blackout health — central to the never-late story — is invisible in the field. | On inter-fix gap crossing a threshold call `gpsLost(sinceLastFixSeconds)`; on next good fix `gpsReacquired(blackoutSeconds, driftMeters)`; emit `ekfHealth(...)` periodically from EKF update. | ✅ Yes — feed replay harness a synthetic gap, assert emit counts/values. |
| 16 | No flush-before-likely-kill path | **HIGH** | `telemetry_service.dart` — no `flush()` anywhere (`grep flush lib/` empty) | No `flush()` on service/sink; nothing forces a durable write before an anticipated kill (backgrounded, low-memory, FGS stop, Doze). With the RAM-only sink, buffered events are lost on the kill telemetry exists to measure. | Add `Future<void> flush()` to sink (no-op default) + `TelemetryService.flush()`; call from `AppLifecycleState.paused/detached` and FGS `onDestroy`/stop. Pair with FileSink fsync. | ✅ Yes — assert JSONL on disk after `emit()+flush()`. |
| 17 | DND: `setBypassDnd` no-op, no ACCESS_NOTIFICATION_POLICY grant/guidance path in Dart | **HIGH** | `android/.../MainActivity.kt:220` & `:248` (`setBypassDnd(true)` in swallowing try/catch); permission declared `AndroidManifest.xml:25`; ZERO `isNotificationPolicyAccessGranted` in `lib/` | `setBypassDnd(true)` is a harmless no-op without the grant, silently swallowed. No Dart reads DND state; preflight has no DND issue code. "DND on + no bypass" is neither detected nor surfaced. | Add `dndBypassGranted` to `ReliabilityProbe` (via native channel), a `PreflightIssueCode.dnd` (block or warn), and `PreflightFixAction.openDndAccessSettings` → `ACTION_NOTIFICATION_POLICY_ACCESS_SETTINGS`. | ✅ Yes — `FakeReliabilityProbe(dndActive, !granted)` → assert issue emitted. |
| 18 | Full-screen intent: check+request exist, but no preflight-warn when FSI unavailable | **HIGH** (partially-fixed) | `notification_service.dart:773`/`:901`/`:916` gate on `canUseFullScreenIntent()`, request path `:630`; native `WakepointNativePlugin.kt:78-83`; `AndroidManifest.xml:13` | The "unchecked" claim is stale — FSI IS gated on a live capability check and a request path exists. What remains: app is not registered as alarm/calendar app, so API 34+ typically returns false → degrades to heads-up banner, with NO preflight warn (no FSI issue code). | Expose `fullScreenIntentAllowed` on `ReliabilityProbe`; add `PreflightIssueCode.fullScreenIntent` at `warn` → `ACTION_MANAGE_APP_USE_FULL_SCREEN_INTENT`. Keep loud-audio+heads-up fallback. | ✅ Yes — probe-driven preflight assertion. |
| 19 | OEM autostart: `_applyFix` collapses every fixAction to `openAppSettings()` | **HIGH** | `reliability_preflight_runner.dart:89-100` (`_applyFix`); real deep-links unused here live at `oem_autostart_service.dart:88-120`/`:125-139`; onboarding-only use `permission_service.dart:39-70` | All four labeled cases fall through to `default: openAppSettings()` — every arm-time "Fix" button drops the user on the generic app-settings page. The per-manufacturer ComponentName deep-link table exists but is used only once at onboarding (gated by `reliability_setup_done`), never per-arm. No re-check on return. | Route each fixAction to its real target (exact-alarm / battery-opt+autostart / notification / location); remove the fall-through default; re-run `ReliabilityPreflightRunner.run()` on dialog resume. | ✅ Yes — unit test switch routing per fixAction. |
| 20 | Battery-opt exemption is not a blocking preflight on aggressive OEMs | **HIGH** | `reliability_preflight_service.dart:307-321` (battery-opt issue = warn/advisory), `:275-285` (only notifications→block), rollup `:346-353`, aggressive needles `:226-242` | On a 5/5 dontkillmyapp ROM the #1 cause of a missed wake (battery-killer not exempted) is at most a dismissible warning, never a hard block. And per #4 even a block verdict is not enforced at the arm gate. | When `aggressive && !batteryExempt`, raise the issue to `PreflightSeverity.block`. Must be paired with #4 (arm-gate enforcement) or it's cosmetic. | ✅ Yes — `FakeReliabilityProbe(batteryExempt:false, oem:'xiaomi')` → assert `level==block`. |
| 21 | ETA sigma collapses to 0 at speed ≤0.5 m/s (underground); no sigma floor | **HIGH** | `lib/services/alarm_evaluator.dart:1538-1560` (returns 0 at `:1548`); duplicated `alarm_controller.dart:1570-1592` (`:1577-1580`) | `etaSigmaSeconds()` returns exactly 0 at any speed ≤0.5 m/s (normal underground stale-speed). Fire test reduces to `etaSeconds <= threshold` — zero statistical cushion, degrades to median firing. | Floor sigma to a positive minimum when speed is unobservable (e.g. `sigmaSMeters/vFloor` with `vFloor≈metroScheduledSpeedMps 9.2`, or a fixed seconds floor). Apply to BOTH copies or share one function. | ✅ Yes — call with `speed=0`, assert sigma > 0. |
| 22 | Metro scheduled-ETA model is dead code; no caller passes `remainingStopsOnMetro`; metro leg degrades to ~0.5 m/s floor underground | **HIGH** | `lib/services/eta_engine.dart:359` (param), `:393-404` (scheduled branch), fallback `:405-413`, `vMin=0.5` `:13`; sole caller `location_stream_handler.dart:359-366` omits the param | `remainingStopsOnMetro` is referenced only inside `eta_engine.dart`; the production call omits it, so the scheduled branch is unreachable. Metro-without-stops falls to `effectiveSpeed=max(vEst, 0.5)`; underground with stale speed ETA = `remaining/0.5` → grossly inflated, fires very late. | Plumb `remainingStopsOnMetro` from the stop-count pipeline into the `computeEta` call; independently raise the metro-without-stops floor off `vMin=0.5` to a transit-realistic speed. | ✅ Yes — call `computeEta` with/without stops, assert scheduled branch runs. |
| 23 | `RouteCache.get()` deletes entry on TTL read → offline reroute impossible after 5 min; no active-route pinning | **HIGH** | `lib/services/route_cache.dart:86` (5-min TTL), `:158-160` (TTL check + delete); `offline_coordinator.dart:166` (get, no ttl override), `:173` (throws StateError), port `:61-68` | `get()` is a destructive read: a cached route older than 5 min is DELETED on read and `getRoute` throws → tracking termination. The read itself evicts the last-good route — gone even if connectivity returns. No pin/ignoreTtl exists. | Add an active-route pin (session key or `pinned:true`) that `get()` ignores TTL/schema/planned-window for; give `RouteCachePort.get` a `Duration? ttl`/`bool pinned`; offline branch passes `pinned:true`. Make reroute read non-destructive. | ✅ Yes — put, fast-forward past 5 min, go offline, call getRoute; assert pinned route survives. |
| 24 | `mapsController.js` caches Google 200-with-error-body as success; auth is a bare public bundle-ID string | **HIGH** | `server .../mapsController.js:24-27` (unconditional `cache.set` + `res.json`), `:28-35` (catch only on HTTP/network error); `authController.js:9-17` (bundleId string-equality) | Google returns HTTP 200 with in-body `{status:'OVER_QUERY_LIMIT'|'REQUEST_DENIED'|...}`; axios doesn't throw, `response.data.status` is never inspected → error body is cached and served to every caller for the TTL. Riders get no route, can't arm. Auth issues a JWT for anyone posting the correct public bundleId (no attestation). | Before `cache.set`, read `response.data.status`: cache only `{OK, ZERO_RESULTS}`, else skip cache and return 502/429. Separately add device attestation before signing the JWT. | ✅ Yes — jest + axios mock returning 200 `{OVER_QUERY_LIMIT}`, assert cache stays empty + non-2xx. |
| 25 | Android 14+ boot/watchdog resume broken: combined FGS type, no live-tracking boot receiver, FGS start not defensively caught | **HIGH** | `AndroidManifest.xml:66` (`foregroundServiceType="location\|mediaPlayback"`), boot receiver only FLN `:80-87`, `RECEIVE_BOOT_COMPLETED :22`; ungated Dart `startService()` `trackingservice.dart:~287-289` | Combined `location\|mediaPlayback` FGS can't be started from a background/boot context on 14+. Only boot receiver re-arms the frozen scheduled notification, not live FGS/EKF tracking. Dart `startService()` isn't wrapped in try/catch — `ForegroundServiceStartNotAllowedException`/ISE propagates. | Split FGS: persistent `location` on tracking service, short-lived `mediaPlayback` only while sounding. Add a custom BOOT_COMPLETED receiver that re-arms tracking from snapshot, catching ISE/FGSNotAllowed. Guard the Dart `startService()` in try/catch with surfaced failure UI. | ❌ No — requires on-device boot/OS-kill. |
| 26 | `hardTMaxSeconds` watchdog + `dwellMinSeconds` topology cap disabled in production wiring | **MED** | `alarm_controller.dart:135-136` (`dwellMinSeconds:0.0`, `hardTMaxSeconds` omitted→null), `:1131` (topology built with `dwellMinSeconds:0.0`); enforced `reachability.dart:259-260` & `:270-272` | Production tracker has `dwellMin=0` and null `hardTMax`. Topology cap is doubly dead; hard T_max watchdog never trips. A 10-20 min blackout with a finite-but-corrupt anchor has no time-budget backstop. | Pass a per-mode `hardTMaxSeconds` blackout budget (e.g. 900-1200s); set a positive `dwellMinSeconds` in BOTH config and the per-tick topology for stopping services. | ✅ Yes — `bound()` with `hardTMax=900, dt=1000` returns watchdog-tripped; `dwellMin>0` capped bound < free-run. |
| 27 | Stop snapping / arc-length projection in raw lat/lng without cos(lat); two snappers disagree | **MED** | `lib/services/stop_matcher.dart:159-179` (esp. `165-178`); `lib/services/polyline_decoder.dart:~226-245` | Both `_projectPointOnSegment` project in raw degree space (dx/dy weighted equally). Snapped point and `t` are skewed E-W on E-W-running segments; no equirectangular correction, unlike `SnapToRouteEngine`. | Apply cos(lat0) scaling to the longitude delta (segment-midpoint lat) before forming dx, undo when reconstructing. Factor into one shared helper so the two can't diverge. | ✅ Yes — project a known point on an E-W segment, assert `t`/snapped point matches corrected math. |
| 28 | Leg city inferred by majority vote of matched stops instead of carried from geocoding | **MED** | `lib/services/transfer_utils.dart:1386-1399` (used at `:1408`) | `enhanceTransitLegStopsWithOsm` builds `cityVotes` over matched OSM stops within 500m and picks max-vote city as `legCityKey`; no geocoded city carried onto the leg. On a border corridor / malformed polyline the vote selects the wrong city → wrong sequence + wrong V_LINE. | Thread the geocoded city onto `TransitLeg` and use it as `legCityKey`; demote majority vote to tiebreaker only. | ✅ Yes — feed a leg with border-ambiguous stops + known geocoded city, assert correct key. |
| 29 | `stopCountConfidence` written but never read by any decision site (G16 unimplemented) | **MED** | `lib/services/transfer_utils.dart:1660` & `:1673` (writes); no read site anywhere in `lib/` or `test/` | Written (0.5 low-confidence / 1.0 geometry-backed), serialized, plumbed through copyWith — but no evaluator/controller reads `leg.stopCountConfidence`. "Prefer early when uncertain" is not implemented. | In the metro stops/ETA path read the active leg's `stopCountConfidence`; when <1.0 widen earliness margin (+1 stop / scale threshold) and/or floor sigma_s upward so a count-divergent leg fires earlier. | ✅ Yes — evaluate a leg with confidence 0.5 vs 1.0, assert earlier fire. |
| 30 | `api_client.dart` bundle-id re-hardcoded inline (false single-source), latent 401-divergence | **MED** | `server-facing api_client.dart:114` (`const bundleId = 'com.geowake.app'`); source of truth `app_config.dart:23`; server rejects mismatch `authController.js:11-17` | `_authenticate()` hardcodes the bundleId as a local const with a "Must match AppConfig.appBundleId" comment instead of importing it. Both currently equal `com.geowake.app` (no live 401), but any future change to `AppConfig.appBundleId` silently 401s every token. | Import and use `AppConfig.appBundleId` at `:114`. | ❌ No — testMode short-circuits `_makeRequest` before `_authenticate`; needs a small refactor to observe, or a source-lint grep for raw bundle-id literals outside `app_config.dart`. |

## Already fixed since GAP_ANALYSIS (doc drift)

These were graded **already-fixed / stale** by the re-verifying agents — GAP_ANALYSIS still lists them as open, so its file:line references have drifted:

- **flutter_local_notifications manifest receivers** (`AndroidManifest.xml:75-90`) — all three FLN v16+ receivers (`ScheduledNotificationReceiver`, `ScheduledNotificationBootReceiver` with BOOT_COMPLETED/QUICKBOOT, `ActionBroadcastReceiver`) are now explicitly declared with a comment. Closes the "process-death alarm backstop was dead" gotcha. (Only makes the *frozen scheduled* backstop survivable — does not resume live tracking; see #25.)
- **Telemetry event schema is PII-free** (`telemetry_service.dart:43`, `:150-261`, `_scrub :268`) — SOUND, not a gap. No helper accepts lat/lng (enforced by construction); coarse rounded durations + booleans only; `recordError` scrubs `/home|/Users` paths; `toJson` sanitizes NaN/Inf. Privacy design delivered.
- **Full-screen-intent "unchecked" claim** — partially stale: the live capability check + special-access request path already exist (`notification_service.dart:773`/`:630`, `WakepointNativePlugin.kt:78-83`). Only the *preflight warn* is missing — retained as #18 at reduced scope.
- **V_LINE RRTS/express detection "unconditionally lost to defaultMps"** — stale: `looksRrts`/`looksExpress` (`reachability.dart:77-101`) now catch RRTS/Namo Bharat/RapidX *when the token is in lineName*. Residual (city never plumbed, unknown→28 not 56, short_name misses) retained as #9.

Also worth flagging as **drifted line numbers** (gap real, cited location moved): reach-bound wiring, backstop id 991, boot/FGS ungated start (`trackingservice.dart` `:211-224` → `~:287-289`), and the `BackgroundService.java:174` SecurityException catch (that catch lives in the external `flutter_background_service` pub-cache, not in-repo — unverifiable here; the in-repo FGS-split + boot-resume gap is confirmed as #25).

## Recommended execution order (single engineer, headless tonight)

Priority: **BLOCK + provable-headless first**, ordered so each ships an independently testable unit. The never-late core (#1-#3) is the product's reason to exist; the telemetry blocks (#7-#8) are cheap and unblock measuring everything else.

1. **#6 `versionCode`** (gradle) — 15 min, unblocks *every* later release. Do it first so nothing you fix tonight is un-shippable.
2. **#4 + #20 preflight enforcement** — capture `preflight`, gate `startTracking` on `!isBlocked`. Small diff, closes the "arms an alarm that can't fire" hole. Then #20 (battery-opt → block) rides on the same enforcement.
3. **#5 cross-state hard block** — delete the gate / soften to a confirm. Small, unblocks flagship interstate journeys.
4. **#1 blackout wall-clock never-late tick** — the core fix. Add the unconditional physics tick + seed the anchor. Largest single lever on the never-late promise.
5. **#2 anchor seeding at arm time** — pairs directly with #1 (the tick needs a seeded anchor). Land together.
6. **#3 hoist reachBound to all fire paths** — extend physics protection to distance / non-metro time / 60% / geofence. Test per-mode.
7. **#7 FileTelemetrySink + configure()** then **#8 alarmOutcome emit** — persistence first, then wire the north-star metric. Unblocks field measurement of everything above.
8. **HIGH provable batch** (any order, each self-contained): #9 V_LINE city+ceiling, #21 ETA sigma floor, #22 metro scheduled-ETA plumbing, #10 backstop lead, #11 backstop 991 cancel, #23 RouteCache pin, #24 mapsController error-body cache, #13 reroute whitelist, #12 metro CI test, #14/#15/#16 telemetry emit sites, #17 DND probe, #18 FSI preflight, #19 OEM `_applyFix` routing.
9. **MED provable**: #26 hardTMax/dwell wiring, #27 cos(lat) projection, #28 leg-city geocoding, #29 stopCountConfidence read.
10. **Defer to on-device** (not headless-provable): #25 Android 14+ boot/FGS split, #30 api_client bundle-id (source-lint instead).

> Rule of thumb tonight: if it's BLOCK and `Provable headless? = ✅`, it ships with a deterministic test before you move on. #25 and #30 are the only two rows you cannot fully prove headless — schedule them for a device pass, not tonight.
