# probe

## Summary
probe summary to test whether the keyFacts array parameter registers correctly in this environment before sending the full payload.

## Key facts
- **probe claim** [high] (as_of ) https://pub.dev/packages/geolocator
