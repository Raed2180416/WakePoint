<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.HIGH_SAMPLING_RATE_SENSORS" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <!-- G1: PARTIAL_WAKE_LOCK for whole tracking session (CPU awake, screen off) -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- G2: prompt user to exempt app from Doze/battery optimization -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- G5: OS-scheduled exact-alarm ETA backstop (survives process death) -->
    <!-- SCHEDULE_EXACT_ALARM only (user grants via the "Alarms & reminders"
         settings toggle, requested in-app). USE_EXACT_ALARM was deliberately
         dropped: Play restricts it to apps whose CORE function is an alarm
         clock/timer/calendar, and a reviewer classifying GeoWake as transit
         tracking-with-an-alarm could reject the declaration late in review.
         One extra settings toggle beats a policy-appeal cycle at launch. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <!-- G4: re-arm background service + backstop after device reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- G9a: allow the alarm channel to bypass Do-Not-Disturb where the user has
         granted notification-policy access (setBypassDnd on the alarm channel) -->
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />

    <application
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:label="GeoWake"
        android:enableOnBackInvokedCallback="true"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules">

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713" />

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:exported="true"
            android:hardwareAccelerated="true"
            android:launchMode="singleTop"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:theme="@style/LaunchTheme"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- GeoWake journey-share deep links (Friends' rides). Two entry
                 points into MainActivity, delivered to app_links →
                 _handleShareLink (lib/main.dart) which follows the ride and
                 opens the "Friends' rides" screen:
                 (1) Verified App Link (https): a tapped /j/{id} link from
                     WhatsApp/SMS opens the app directly (no chooser) once the
                     host serves /.well-known/assetlinks.json listing THIS
                     build's signing SHA-256. The host must match the App-Links
                     domain (ShareLinkBuilder.defaultDomain / GEOWAKE_SHARE_DOMAIN).
                 (2) Custom scheme geowake://j/{id}: always opens the app WITHOUT
                     assetlinks verification — the interim/testing entry point. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="geowake-share-production.up.railway.app"
                    android:pathPrefix="/j" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="geowake" android:host="j" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${googleMapsApiKey}" />

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <service
            android:name="id.flutter.flutter_background_service.BackgroundService"
            android:foregroundServiceType="location|mediaPlayback" />

        <!-- CRITICAL RELIABILITY FIX: flutter_local_notifications v16+ no longer
             auto-declares these receivers. Without them, zonedSchedule(
             AndroidScheduleMode.alarmClock) — the exact-alarm ETA backstop that
             must wake the user after TOTAL process death (notification_service
             .dart:880) — is NEVER delivered (AlarmManager fires the intent but
             no receiver catches it). The boot receiver re-arms scheduled
             backstops after a reboot. See flutter_local_notifications README. -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ActionBroadcastReceiver" />

        <!-- GeoWake home-screen widget (FEATURES_SPEC §3.6-A). Display/observer
             surface only: renders the state HomeWidgetBridge mirrors in and, on
             tap, LAUNCHes MainActivity with the gw_widget_deeplink URI so the
             normal Dart one-tap-arm flow runs. Never on the arm→track→alarm
             path. android:name matches HomeWidgetBridge.androidQualifiedName
             (com.example.geowake2.GeoWakeWidgetProvider). Additive — leaves the
             notification/boot receivers and deep-link intent-filters untouched. -->
        <receiver
            android:name=".GeoWakeWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/geowake_widget_info" />
        </receiver>

        <!-- home_widget background plumbing for registerInteractivityCallback /
             widgetInteractivityCallback. Inert unless a background widget intent
             is dispatched; the widget's taps use a LAUNCH intent, so these are a
             standard fail-safe fallback, not the primary path. -->
        <receiver
            android:name="es.antonborri.home_widget.HomeWidgetBackgroundReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="es.antonborri.home_widget.action.BACKGROUND" />
            </intent-filter>
        </receiver>
        <service
            android:name="es.antonborri.home_widget.HomeWidgetBackgroundService"
            android:permission="android.permission.BIND_JOB_SERVICE"
            android:exported="true" />

    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
        <!-- Guardian mode: compose an "arrived safely" / tracking-link message into
             the user's SMS or WhatsApp app (USER taps send). Android 11+ package
             visibility so url_launcher can resolve these composers. -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="sms" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="smsto" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <!-- G2: allow querying/launching the OS battery-optimization settings screen -->
        <intent>
            <action android:name="android.settings.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
        </intent>
        <intent>
            <action android:name="android.settings.IGNORE_BATTERY_OPTIMIZATION_SETTINGS" />
        </intent>
        <!-- G2: OEM autostart / background-permission manager packages (Android 11+ package visibility) -->
        <package android:name="com.miui.securitycenter" />
        <package android:name="com.coloros.safecenter" />
        <package android:name="com.oplus.safecenter" />
        <package android:name="com.oppo.safe" />
        <package android:name="com.iqoo.secure" />
        <package android:name="com.vivo.permissionmanager" />
        <package android:name="com.huawei.systemmanager" />
        <package android:name="com.oneplus.security" />
        <package android:name="com.samsung.android.lool" />
        <package android:name="com.samsung.android.sm" />
    </queries>
</manifest>